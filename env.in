LIB_MODE=-DX86_64

ifeq (,$(PRO_DIR))
  PRO_DIR =$(shell pwd)
endif

OPath=$(PRO_DIR)
LIB_PATH=/home/liu/fslib

RM=rm
MV=mv
CD=cd
CP=cp


LIBS += -lnsl -lm -lrt -ldl
LIBS += -pthread

CFLAGS = -Wall -Werror
CFLAGS += -g
CFLAGS += $(LIB_MODE) $(WORK_MODE)
CFLAGS += $(LIBINC) $(PROG_INC)

